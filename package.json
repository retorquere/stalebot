{
  "name": "stalebot",
  "version": "1.0.2",
  "main": "index.js",
  "scripts": {
    "start": "ncc build index.js -o dist",
    "pretest": "npm start",
    "test": "dprint fmt index.js",
    "preversion": "npm start",
    "version": "git add dist",
    "postversion": "node scripts/sync-major-tag.js"
  },
  "dependencies": {
    "@actions/core": "^3.0.1",
    "@actions/github": "^9.1.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.44.1",
    "dprint": "^0.55.1"
  }
}
